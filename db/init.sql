-- Base schema is created by SQLAlchemy on first boot (see app/init_db.py).
-- This file is kept for any pure-SQL bootstrap needs (extensions, roles, etc.).
CREATE EXTENSION IF NOT EXISTS "pgcrypto";
